# Yodo1 MAS ANE - Android Lib Dependencies

Place `FlashRuntimeExtensions.jar` from your AIR SDK here:

```bash
cp $AIR_SDK_PATH/lib/android/FlashRuntimeExtensions.jar ./
```

This JAR is required to compile the native Android library but is NOT
packaged into the final ANE (AIR runtime provides it at runtime).
